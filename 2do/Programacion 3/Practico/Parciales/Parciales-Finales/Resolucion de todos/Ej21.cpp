/* [Ejercicio 21]: no salio
Mostrar a través de la implementación de una clase el funcionamiento de una
estructura de datos Cola utilizando exclusivamente la estructura de datos Pila vista en
clase. (pista: deben utilizar métodos de la clase Pila, y no hace falta que la clase cuente
con un atributo de tipo Nodo). La clase debe contar con los métodos:
• push(): permite insertar un valor en la estructura.
• pop(): permite quitar un valor de la estructura (debe quitar el valor más viejo
en la estructura ya que debe responder al principio FIFO).
• esVacia(): devuelve verdadero si la estructura no tiene elementos o falso si esta
tiene elementos.
• Constructor y Destructor apropiados.
• Más métodos que se les ocurran (que sean relevantes al funcionamiento de la
estructura) se considerarán como bonificación extra.
*/

